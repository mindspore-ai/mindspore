#operator reduce_sum
reduce_sum:
  args:
    x:
      dtype: tensor
    axis:
      dtype: tuple[int]
      type_cast: int, list[int]
    keep_dims:
      dtype: bool
      init: False
    skip_mode:
      dtype: bool
      init: False
  returns:
    output:
      dtype: tensor
