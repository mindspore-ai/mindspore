#operator bias_add_grad
bias_add_grad:
    args:
        dout:
            dtype: tensor
        data_format:
            dtype: int
            init: "'NCHW'"
            arg_handler: format_to_enum
    returns:
        output:
            dtype: tensor
