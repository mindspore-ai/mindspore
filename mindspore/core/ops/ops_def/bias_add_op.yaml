#operator bias_add
bias_add:
    args:
        input_x:
            dtype: tensor
        bias:
            dtype: tensor
        data_format:
            dtype: int
            init: "'NCHW'"
            arg_handler: format_to_enum
    returns:
        output:
            dtype: tensor
