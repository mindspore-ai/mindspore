#operator bias_add
bias_add:
    args:
        input_x:
            dtype: tensor
        bias:
            dtype: tensor
        data_format:
            dtype: int
            default: "'NCHW'"
            prim_init: True
            arg_handler: py_format_to_enum
    returns:
        output:
            dtype: tensor
