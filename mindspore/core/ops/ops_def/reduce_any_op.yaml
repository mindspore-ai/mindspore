#operator reduce_any
reduce_any:
  args:
    x:
      dtype: tensor
    axis:
      dtype: tuple[int]
      type_cast: int, list[int]
      default: ()
    keep_dims:
      dtype: bool
      init: False
  returns:
    output:
      dtype: tensor
