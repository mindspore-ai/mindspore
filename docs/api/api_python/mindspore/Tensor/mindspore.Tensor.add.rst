mindspore.Tensor.add
====================

.. py:method:: mindspore.Tensor.add(y)

    逐元素相加。

    .. math::
        output[i] = x[i] + y[i]

    参数：
        - **y** (Tensor) - 要相加的张量。

    返回：
        Tensor，shape和数据类型与当前Tensor相同。
