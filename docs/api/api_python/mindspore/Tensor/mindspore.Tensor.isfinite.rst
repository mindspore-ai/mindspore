mindspore.Tensor.isfinite
=========================

.. py:method:: mindspore.Tensor.isfinite()

    判断输入数据每个位置上的值是否是有限数。

    返回：
        Tensor，输出的shape与输入相同，数据类型为bool。

    异常：
        - **TypeError** - 如果当前操作数不是Tensor。
