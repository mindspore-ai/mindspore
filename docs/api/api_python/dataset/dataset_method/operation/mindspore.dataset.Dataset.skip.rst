mindspore.dataset.Dataset.skip
===============================

.. py:method:: mindspore.dataset.Dataset.skip(count)

    跳过此数据集对象的前 `count` 条数据。

    参数：
        - **count** (int) - 要跳过数据的条数。

    返回：
        Dataset，应用了上述操作的新数据集对象。
