mindspore.dataset.Dataset.use_sampler
=====================================

.. py:method:: mindspore.dataset.Dataset.use_sampler(new_sampler)

    替换当前数据集的最末子采样器，保持父采样器不变。

    参数：
        - **new_sampler** (Sampler) - 用于替换的新采样器。
