mindspore.ops.numel
====================

.. py:function:: mindspore.ops.numel(input)

    返回Tensor的元素的总数量。

    参数：
        - **input** (Tensor) - 输入Tensor。

    返回：
        int。Tensor的元素的总数量。

    异常：
        - **TypeError** - `input` 不是Tensor。
