mindspore.ops.Abs
==================

.. py:class:: mindspore.ops.Abs

    逐元素计算输入Tensor的绝对值。
    
    更多详情请查看： :func:`mindspore.ops.abs` 。

    输入：
        - **x** (Tensor) - 输入Tensor。其shape为 :math:`(N,*)` ，其中 :math:`*` 表示任意数量的附加维度。

    输出：
        Tensor，shape与 `x` 相同。
