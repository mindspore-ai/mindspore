mindspore.ops.Igammac
=====================

.. py:class:: mindspore.ops.Igammac

    计算正则化的上层不完全伽马函数。

    更多参考详见 :func:`mindspore.ops.igammac`。

    输入：
        - **a** (Tensor) - 输入Tensor。
        - **x** (Tensor) - 输入Tensor，数据类型应该与 `a` 一致。

    输出：
        Tensor，数据类型与 `a` 和 `x` 相同。
