mindspore.ops.ravel
======================

.. py:function:: mindspore.ops.ravel(input)

    返回一个展开的一维Tensor。

    参数：
        - **input** (Tensor) - 要展开的Tensor。

    返回：
        一维Tensor，含有与输入相同的元素。

    异常：
        - **TypeError** - 如果 `input` 不是Tensor。