mindspore.ops.Expm1
====================

.. py:class:: mindspore.ops.Expm1

    返回Tensor元素的指数，然后减去1。

    更多参考详见 :func:`mindspore.ops.expm1` 。

    输入：
        - **x** (Tensor) - 输入Tensor。

    输出：
        Tensor，与输入 `x` 具有相同的shape和数据类型。
