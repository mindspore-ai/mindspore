mindspore.ops.ScatterNdMin
===========================

.. py:class:: mindspore.ops.ScatterNdMin

    对张量中的单个值或切片应用sparse minimum。

    使用给定值通过最小值运算和输入索引更新 `input_x` 的值。为便于使用更新后的值，函数返回 `input_x` 的副本。

    更多参考详见 :func:`mindspore.ops.scatter_nd_min` 。
